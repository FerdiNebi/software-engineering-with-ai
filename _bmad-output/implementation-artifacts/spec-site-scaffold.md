---
title: 'Scaffold Astro Starlight site (walking skeleton)'
type: 'feature'
created: '2026-04-23'
status: 'in-progress'
baseline_commit: '403fe6899c9c4d4c8cbd10ce418b792bf5b511bb'
context:
  - '_bmad-output/planning-artifacts/architecture.md'
  - '_bmad-output/planning-artifacts/prd.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** The repo contains only planning artifacts — no Astro scaffold, no content tree, no deploy pipeline. No page can be authored or deployed until the walking skeleton exists.

**Approach:** Execute the 10-step scaffold sequence pinned in `architecture.md` (Core Architectural Decisions → Decision Impact Analysis). Ship a buildable, deployable Starlight site with 43 empty page stubs wired into an explicit sidebar and a GitHub Actions deploy workflow. No content bodies.

## Boundaries & Constraints

**Always:**
- Follow `architecture.md` decisions verbatim: Astro Starlight, TypeScript strict, Node 20 LTS, pnpm, `withastro/action@v3`, GitHub Pages.
- Use the pinned directory tree and phase slugs from architecture.md (`pre-sales`, `discovery`, `requirements-design`, `development`, `qa-testing`, `deployment-launch`, `maintenance-retainer`).
- Use the pinned frontmatter schema (`title`, `description`, `type`, `phase`, `order`, `lastUpdated`) on every page stub.
- Base path: `site: 'https://ferdinebi.github.io'`, `base: '/software-engineering-with-ai/'`.
- Sidebar is explicit in `astro.config.mjs` — not filesystem-autogenerated.
- Every page stub contains the four required H2 headings in order: `What happens here`, `Best practices`, `Desired outcomes`, `What the industry does`. Body under each heading is a single TODO placeholder line.

**Ask First:**
- If `pnpm create astro` prompts for options not covered by the `--template starlight --typescript strict` flags.
- If the repo `FerdiNebi/software-engineering-with-ai` does not yet exist on GitHub (needed for base-path URL to resolve post-deploy).

**Never:**
- Write real content in page stubs (that's separate quick-dev cycles).
- Add custom CSS, brand palette, or override Starlight theme defaults.
- Add React/Vue/other framework islands (none needed for v1).
- Add remark-lint, broken-link-check, or other build-time validators beyond the Zod schema.
- Commit `node_modules/`, `dist/`, or `.astro/`.
- Touch `_bmad/` or `_bmad-output/` beyond this spec file.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Clean build | `pnpm install && pnpm build` on clean clone | `dist/` generated with 43 routes, 0 errors, under 60s | N/A |
| Missing H2 in stub | Page omits one of 4 required H2s | Build passes (v1: convention-only); authoring convention violation caught in PR review | Flag in review |
| Invalid frontmatter | Page has unknown field or missing required field | Zod schema fails build | Build error |
| Slug mismatch in sidebar | Sidebar entry references non-existent slug | Starlight surfaces as 404 during build verification | Build or dev-server error |
| Base-path hardcoded | Internal link uses absolute GitHub Pages URL | Link rots if base changes; functions but is an anti-pattern | PR review |

</frozen-after-approval>

## Code Map

- `package.json` -- pnpm pin, scripts (`dev`, `build`, `preview`), Starlight + `@astrojs/sitemap` deps
- `astro.config.mjs` -- `site`, `base`, Starlight integration, sitemap integration, explicit sidebar for all 43 pages
- `src/content/config.ts` -- Zod schemas for `phase-overview` and `sub-section` collections (discriminant: `type`)
- `src/content/docs/index.mdx` -- home/landing stub
- `src/content/docs/<phase>/index.md` x7 -- phase overview stubs
- `src/content/docs/<phase>/<sub-section>.md` x35 -- sub-section stubs (names per architecture.md directory tree)
- `.github/workflows/deploy.yml` -- `withastro/action@v3` on push to `main`
- `.nvmrc` -- `20`
- `tsconfig.json` -- strict, path aliases (`@components/*`, `@content/*`)
- `.gitignore` -- `node_modules/`, `dist/`, `.astro/`, `.env*`, OS cruft
- `public/favicon.svg`, `public/robots.txt` -- default placeholders acceptable

## Tasks & Acceptance

**Execution:**
- [ ] Run `pnpm create astro@latest -- --template starlight --typescript strict` at repo root; accept defaults that don't conflict with constraints; answer **Ask First** prompts if they appear
- [ ] `astro.config.mjs` -- set `site: 'https://ferdinebi.github.io'`, `base: '/software-engineering-with-ai/'`; add `@astrojs/sitemap` integration; replace default sidebar with explicit 7-phase sidebar referencing all 43 slugs in PRD/architecture order
- [ ] `src/content/config.ts` -- define Zod schemas for `phase-overview` and `sub-section` collections matching architecture.md frontmatter spec
- [ ] `src/content/docs/index.mdx` -- home stub (title, description, short intro paragraph explaining the site purpose, sidebar visible)
- [ ] `src/content/docs/<phase>/index.md` x7 -- phase overview stubs with required frontmatter + four H2 headings + TODO lines
- [ ] `src/content/docs/<phase>/<sub-section>.md` x35 -- sub-section stubs with required frontmatter + four H2 headings + TODO lines (filenames from architecture.md directory tree)
- [ ] `.github/workflows/deploy.yml` -- checkout → setup-pnpm → setup-node (`.nvmrc`) → `pnpm install --frozen-lockfile` → `pnpm build` → `withastro/action@v3` publish
- [ ] `.nvmrc` -- `20`; `package.json` -- add `packageManager: "pnpm@<version>"` field
- [ ] `tsconfig.json` -- path aliases `@components/*`, `@content/*` (verify scaffold defaults; amend if missing)
- [ ] `.gitignore` -- append `dist/`, `.astro/`, `node_modules/`, `.env*` (verify Astro defaults; amend if missing)

**Acceptance Criteria:**
- Given a clean clone, when `pnpm install && pnpm build` runs, then it succeeds in under 60s and produces `dist/` with 43 HTML routes.
- Given the dev server, when navigating to `/` under the configured base, then the sidebar shows all 7 phases with correct ordering and all 35 sub-sections are clickable.
- Given any stub page, when loaded, then the frontmatter passes Zod validation and the rendered page contains the four required H2 headings in the specified order.
- Given a push to `main`, when the GitHub Actions workflow runs, then it deploys to GitHub Pages at `https://ferdinebi.github.io/software-engineering-with-ai/`.
- Given dark mode toggle, when toggled, then both modes render legibly (Starlight default contrast).

## Verification

**Commands:**
- `pnpm install` -- expected: installs without peer-dep errors
- `pnpm build` -- expected: exits 0; logs 43 routes built; duration <60s
- `pnpm dev` -- expected: starts on `http://localhost:4321/software-engineering-with-ai/`; all sidebar entries resolve without 404s

**Manual checks:**
- Confirm `dist/` contains the 43 expected HTML files under the phase folder layout.
- Confirm `.github/workflows/deploy.yml` uses `withastro/action@v3` and triggers only on `main`.
- After first merge to `main`, confirm GitHub Pages serves the landing page at the expected URL.
